---
import { Image } from "astro:assets";
import { hosts } from "../data/hosts";
---

<div class="grid gap-6 sm:grid-cols-2">
  {
    hosts.map((host) => (
      <a
        href={`/about#${host.slug}`}
        class="group flex items-center gap-4 rounded-2xl border border-border bg-surface p-4 transition-colors hover:border-accent"
      >
        <Image
          src={host.photo}
          alt={host.name}
          width={56}
          height={56}
          class="size-14 shrink-0 rounded-full object-cover"
        />
        <div class="min-w-0">
          <p class="font-display text-lg leading-tight group-hover:text-accent">{host.name}</p>
          <p class="eyebrow mt-1 truncate text-fg-muted">{host.role}</p>
        </div>
      </a>
    ))
  }
</div>
